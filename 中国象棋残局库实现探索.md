# 中国象棋残局库实现探索：核心概念与接力指南

> **状态**：核心流程已验证，核心阻塞问题已复现，欢迎接力开发  
> **最后更新**：2026-07-15  
> **核心验证局面**：`3ak1r2/4aP3/9/5R3/9/9/9/9/9/4K4 w - - 0 1`

---

## 一、背景与目标

原生象眼引擎对残局的支持依赖暴力深度搜索，无人类残局认知（如同质局面等价、步数最优、错着应对），且移动端算力有限。本探索的目标是：

- **实现残局专项能力**：轻量逻辑适配移动端，避免重搜索，复用象眼成熟搜索能力。
- **一个残局覆盖一批同质局面**：降低残局库存储和维护成本，无需为同质局面重复存着法。
- **着法可达性保障**：确保库着法在实际局面中可合法执行，无阻挡问题。
- **绝杀步数量化透传**：向应用层输出残局绝杀所需的最少步数，提升交互体验。

---

## 二、核心概念（已验证/待探索明确区分）

### 1. 归一化（Normalization）——同质局面等价映射（部分已验证，核心阻塞待解决）

**定义**：将同一残局的所有同质化局面映射到同一个标准局面（`stdFen`），实现“一个残局覆盖一批局面”。

**价值**：残局库仅需存储标准局面的着法，无需为车在`(4,5)`、`(5,5)`等同质局面重复存储，大幅降低库体积。

✅ **已验证环节（实锤可行）**：使用测试局面 `3ak1r2/4aP3/9/5R3/9/9/9/9/9/4K4 w - - 0 1` 验证通过：
- 可正确生成同质化标准局面（`stdFen`）；
- 归一化后的着法可正常写入残局库；
- 搜索时可精准命中标准局面的着法，返回合法结果。

❌ **后续发现的实锤问题（核心阻塞点）**：当前归一化逻辑仅完成坐标变换，**未校验目标位置/路径阻挡**，导致归一化后的着法在原始非标准局面中可能不可达（比如目标位置有子、车炮路径被挡），该问题已在实际测试中复现，待攻关解决。

---

### 2. 绝杀步数存储——两种探索思路（无绝对优劣，适配场景不同）


#### 方案A：权重字段高位共享方案（零存储开销）
复用开局库 `book.dat` 中权重字段的后2字节（共16位），拆分为 **高5位 + 低11位**：
- **高5位**：存储绝杀步数，取值范围0-31，常规残局场景0-20步已完全足够。
- **低11位**：存储着法权重，取值范围0-55535。

可根据实际需求灵活调整位宽拆分比例（如6+10、4+12等），无需新增文件结构，完全兼容现有开局库生态，运行时无额外性能损耗，适合嵌入式设备等算力受限场景。

#### 方案B：动态BFS搜索方案（动态计算绝杀步数）
引擎搜索阶段动态运行BFS算法，实时计算最短绝杀路径，通过JNI透传到App层。绝杀步数无理论上限，精度高，与权重逻辑完全解耦，适合安卓/iOS等算力充足的移动端、需要精准长步数绝杀展示的场景。

---

### 3. 镜像（Mirror）——突破引擎子力阵营限制的构思（未验证）

**定义**：通过坐标变换 `行=9-行`、`列=8-列`，将任意残局局面转换为引擎认知的「红下黑上、红左黑右」标准朝向。

**价值**：原生象眼硬编码了标准朝向假设，非标准朝向局面无法命中残局库。镜像本质是“适配引擎认知”的变换手段，让引擎识别非标准局面。

**当前状态**：仅为前期构思，**未编写任何相关代码，未进行实际验证**，能否适配现有归一化逻辑、是否会引入新的坐标冲突均有待探索。

---

## 三、核心阻塞问题（已复现，待接力攻关）

### 归一化时的有子阻挡校验（已确认存在，未实现）

当前归一化流程已完成 `stdFen` 生成、库写入、搜索命中的验证，但**未校验两类阻挡场景**，该问题已在实际测试中复现，是后续开发的核心卡点：

1. **目标位置阻挡**：归一化后的目标落子位置或路径中在原始局面中存在己方子力或对方子力，导致着法无法执行。
2. **路径阻挡**：车、炮等直线走子的路径在原始局面中存在其他子力阻挡，不符合棋子走法规则。

> **示例**：红车原始位置 `(4,5)或(3,5)`，归一化后目标位置 `(5,5)`，但原始局面中 `(5,4)或(5,3)` 有子，或红车目标位置 `(5,2)` 有己方棋子，则该归一化着法无效。

---

## 四、待接力方向（优先级排序）

1. **【最高优先级】攻关有子阻挡校验逻辑**：实现归一化前的可达性校验，确保输出的着法100%合法。
2. **验证镜像构思可行性**：若后续尝试镜像方案，需先验证其与归一化逻辑的兼容性，避免引入新的坐标冲突。
3. **完善不同残局类型的归一化规则**：针对车、炮、兵等常见残局子力，梳理各自的归一化映射规则，覆盖80%常见残局。
4. **着法逆变换适配**：实现归一化着法到原始局面的坐标逆变换，确保引擎输出的着法符合用户视角的坐标系。
5. **性能优化**：针对归一化校验逻辑做剪枝，适配移动端性能要求。
6. **绝杀步数存储方案选型**：根据实际场景需求，可选择权重字段高位共享方案优化存储开销，或动态BFS方案优化展示精度，也可探索结合镜像归一化的混合方案。

---

## 五、如何接力

1. **从核心阻塞问题入手**：先针对测试局面 `3ak1r2/4aP3/9/5R3/9/9/9/9/9/4K4 w - - 0 1` 实现阻挡校验逻辑，验证后可扩展到其他残局类型。
2. **参考现有草稿代码**：`eleeye/book_endgame_normalizer.cpp` 已包含归一化的初步坐标变换实现，可直接在此基础上修改。
3. **复用已验证的调参经验**：少子力（≤4子）残局建议搜索深度10级（上限20级），避免移动端卡顿。
4. **回溯历史提交**：所有代码变更已归档至本仓库提交历史，可直接参考过往探索的尝试与踩坑记录。
5. **尝试绝杀存储方案**：若选择权重高位共享方案，可直接修改 `eleeye/book.cpp` 的编码/解码逻辑，调整位宽拆分比例适配你的场景需求。

---

## 附录：核心验证数据

| 测试场景 | 结果 |
| :--- | :--- |
| 归一化生成 `stdFen` | ✅ 已验证，可正确生成标准局面 |
| 归一化着法写入残局库 | ✅ 已验证，无写入异常 |
| 归一化后着法搜索命中 | ✅ 已验证，返回合法着法 |
| **归一化后着法阻挡校验** | **❌ 未实现，已复现问题：目标/路径有阻挡时着法不可达** |
| 镜像构思验证 | ⏳ 未编写代码、未验证，仅为前期想法 |
| 绝杀步数动态BFS方案 | ✅ 已验证，精度高，适配移动端场景 |
| 绝杀步数权重高位共享方案 | ✅ 已验证位宽拆分逻辑，零存储开销 |
