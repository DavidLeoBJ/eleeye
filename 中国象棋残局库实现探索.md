# 中国象棋残局库实现探索：核心概念与接力指南
> 状态：核心概念已验证，核心阻塞问题待解决，欢迎接力开发
> 最后更新：2026-07-15
> 核心验证局面：`3ak1r2/4aP3/9/5R3/9/9/9/9/9/4K4 w - - 0 1`

## <span style="color:#165DFF">一、背景与目标</span>
原生象眼引擎对残局的支持依赖暴力深度搜索，无人类残局认知（如同质局面等价、步数最优、错着应对），且移动端算力有限。本探索的目标是：
1.  实现残局专项能力：轻量逻辑适配移动端，避免重搜索，复用象眼成熟搜索能力。
2.  一个残局覆盖一批同质局面：降低残局库存储和维护成本，无需为同质局面重复存着法。
3.  着法可达性保障：确保库着法在实际局面中可合法执行，无阻挡问题。

## <span style="color:#165DFF">二、核心概念（已验证，是残局实现的基础）</span>
### 1. <span style="color:#165DFF">镜像（Mirror）——突破引擎子力阵营限制</span>
- **定义**：通过坐标变换`行=9-行`、`列=8-列`，将任意残局局面转换为引擎认知的「红下黑上、红左黑右」标准朝向。
- **价值**：原生象眼硬编码了标准朝向假设，非标准朝向局面无法命中残局库。镜像本质是“骗引擎”的伪装手段，让引擎识别非标准局面。

### 2. <span style="color:#165DFF">归一化（Normalization）——同质局面等价映射</span>
- **定义**：将同一残局的所有同质化局面映射到同一个标准局面，实现“一个残局覆盖一批局面”。
- **价值**：残局库仅需存储标准局面的着法，无需为车在(4,5)、(5,5)等同质局面重复存储，大幅降低库体积。
- **已验证成果**：使用测试局面`3ak1r2/4aP3/9/5R3/9/9/9/9/9/4K4 w - - 0 1`验证：
  归一化后着法可正常写入残局库，搜索时可精准命中并返回合法着法，核心逻辑已跑通。

## <span style="color:#F53F3F">三、核心阻塞问题（待接力攻关）</span>
### <span style="color:#F53F3F">归一化时的有子阻挡校验（当前未解决）</span>
当前归一化逻辑仅做坐标变换，未校验以下两类阻挡场景，会导致归一化后的着法在原始局面中**不可达**，是后续开发的核心卡点：
1.  **目标位置阻挡**：归一化后的目标落子位置存在己方/对方子力，导致着法无法执行。
2.  **路径阻挡**：车、炮等直线走子的路径上存在其他子力阻挡，不符合棋子走法规则。
> 示例：若将红车归一化到某目标位置，但该位置已有黑士，或红车到目标的直线路径上有红兵阻挡，则该归一化着法无效。

## <span style="color:#165DFF">四、待接力方向（优先级排序）</span>
1.  **攻关有子阻挡校验逻辑**：实现归一化前的可达性校验，确保输出的着法100%合法。
2.  **完善不同残局类型的归一化规则**：针对车、炮、兵等常见残局子力，梳理各自的归一化映射规则，覆盖80%常见残局。
3.  **着法逆变换适配**：实现归一化着法到原始局面的坐标逆变换，确保引擎输出的着法符合用户视角的坐标系。
4.  **性能优化**：针对归一化校验逻辑做剪枝，适配移动端性能要求。

## <span style="color:#165DFF">五、如何接力</span>
1.  从核心阻塞问题「有子阻挡校验」入手，先针对测试局面`3ak1r2/4aP3/9/5R3/9/9/9/9/9/4K4 w - - 0 1`实现校验逻辑，验证后可扩展到其他残局类型。
2.  参考`eleeye/book_endgame_normalizer.cpp`中的草稿代码，该文件已包含归一化的初步坐标变换实现。
3.  复用已验证的深度调参经验：少子力（≤4子）残局建议搜索深度10级（上限20级），避免卡顿。
4.  所有代码变更已归档至本仓库提交历史，可直接回溯参考。

---
## <span style="color:#165DFF">附录：核心验证数据</span>
| 测试场景 | 结果 |
|----------|------|
| 测试局面`3ak1r2/4aP3/9/5R3/9/9/9/9/9/4K4 w - - 0 1`归一化 | 着法可正常写入残局库 |
| 归一化后着法搜索 | 可精准命中，返回合法着法 |
| 有子阻挡场景处理 | <span style="color:#F53F3F">未实现，为当前核心阻塞问题</span> |